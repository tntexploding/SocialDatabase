# 变更记录

本项目从 `0.4.0` 起采用语义化版本号。日期使用 `YYYY-MM-DD`，仅记录已经进入
对应版本的用户可见能力、数据兼容变化与维护事项。

## 0.8.1 - 2026-08-27

### 新增

- 增加受 Bearer 认证的 `POST /api/v1/query-text`，使用 JSON 请求体并返回适合
  QQ/LAS 转发的摘要纯文本。

### 安全与约束

- 固定消息查询的结果数量、每用户群组数量和总字符上限，避免宽泛查询产生超长
  消息；无结果响应不回显原始查询词。

## 0.8.0 - 2026-08-25

### 新增

- 将 AstrBot `aiocqhttp` 采集器分离到独立的
  `astrbot_plugin_socialdatabase` 仓库；本仓库只保留版本化 HTTP JSON v1
  接收、认证和合并接口。
- 明确插件与服务端不存在 Python 代码依赖，跨仓库只通过
  `POST /api/v1/imports/json` 和稳定 `producer + batch_id` 通信。
- HTTP 服务支持当前令牌与一个临时旧令牌，形成可验证的无停机轮换窗口。
- 增加 Caddy 生产 Compose、自动 HTTPS、来源 CIDR 限制、内外网络隔离和生产
  环境模板，以及云端部署、升级、备份和轮换手册。
- 增加不记录令牌和成员资料的真实环境联合调试清单，覆盖插件热重载、离线队列、
  HTTP 幂等与历史关系保留。

### 修复与改进

- 增加写事务保持期间并发读取、并发 HTTP 导入串行化和模拟导入中断完整回滚
  测试；所有测试数据继续只写入 pytest 临时目录。
- 明确外部生产方成功上传不要求服务端之外的成功归档，服务端导入历史作为权威
  记录；超时后用相同 payload 与稳定批次身份重试。
- 根据当前负载继续采用服务端同步单写模型；持久重试位于数据生产方一侧，暂不
  引入缺少实际排队依据的第二套服务端任务队列。

### 兼容性

- 数据库继续使用 schema 4，没有迁移或历史数据改写；xlsx、文件 JSON、CLI 和
  0.7.0 HTTP 客户端保持兼容。
- AstrBot、OneBot 与 `aiohttp` 不进入 SocialDatabase 的 wheel、sdist、容器或
  开发依赖；插件兼容范围和依赖由其独立仓库维护。
- 自动定时采集、从缺席推断退群、多个 API 实例共同写 SQLite 仍不属于当前
  范围；真实云主机和机器人联调需由部署者提供外部环境。

## 0.7.0 - 2026-08-25

### 新增

- 引入 schema 4 和可选 JSON v1 `batch_id`；`producer + batch_id` 形成稳定
  幂等身份，同一身份对应不同内容时明确拒绝。
- 增加受 Bearer 令牌保护的 HTTP API，提供搜索、统计、导入历史、JSON 批次
  导入和数据库健康检查，以及无需认证的存活/就绪探针。
- 增加 `serve` 命令、独立服务依赖清单、非 root Docker 镜像、Compose 持久卷
  和容器健康检查。
- HTTP JSON 直接进入格式无关合并核心，不在项目或数据目录创建上传临时文件。

### 修复与改进

- HTTP 上传使用规范化 JSON 哈希，使未提供 `batch_id` 的相同语义请求不受键
  顺序和空白变化影响。
- 服务使用单 Uvicorn worker、进程内写入锁、SQLite WAL 与忙等待；默认关闭
  可能记录搜索查询参数的访问日志。
- API 统一限制请求体和记录数量，不向响应公开服务端数据库路径。

### 兼容性

- JSON v1 的 `batch_id` 保持可选；0.6.0 文件和原有 xlsx/CLI 用法继续可用。
- HTTP 依赖通过 `social-database[server]` 或 `requirements-server.txt` 安装，
  纯 CLI 运行依赖保持精简。
- 自动采集、AstrBot 插件本体、成员状态推断和内置 HTTPS 不属于本版本范围。

## 0.6.0 - 2026-08-25

### 新增

- 引入 schema 3，保存实际数据源中的 11 个扩展成员—群组字段，同时保持旧版
  八列表格兼容。
- 增加版本化标准 JSON v1 批次格式和 `import-json` 命令；xlsx 与 JSON 共用
  同一套非破坏合并、来源哈希、批次记录与搜索索引同步逻辑。
- 导入批次可记录数据生产方、格式版本与实际采集时间。
- 搜索结果和 JSON、CSV、xlsx 导出增加完整扩展字段，以及关系首次/最近观察
  批次和时间。
- 扩展字段支持显式字段搜索；原有 `any` 搜索范围保持不变。

### 修复与改进

- 数据库会在任何 `create_all` 或迁移操作前只读检查 schema 版本，未来版本
  数据库不再可能先被当前程序修改再报错。
- 非搜索字段单独变化时不再触发无意义的 FTS5 全量重建。
- 明确 JSON/AstrBot 适配边界：核心项目仍不获取数据，也不根据批次缺席推断
  退群或删除历史关系。

### 兼容性

- 继续支持 Python 3.10 及以上版本。
- schema 2 数据库自动无损迁移到 schema 3；新增列均允许空值。
- HTTP 服务、Docker、自动采集和 AstrBot 插件本体不属于本版本范围。

## 0.5.0 - 2026-08-24

### 新增

- 引入 schema 2 和可重建的 contentful FTS5 trigram 搜索索引。
- 增加 FTS5/LIKE 混合路由：长度至少为 3 的适用查询使用 FTS5，短词、群 ID
  查询或索引不可用时自动回退 LIKE。
- 增加 `reindex` 命令，以及 FTS5 内部完整性、行数和内容一致性检查。
- 增加隐私安全的实际库性能基准和隔离式 FTS5 原型工具。
- 增加稳定版构建依赖、CI 分发包构建步骤和发布检查清单。

### 修复与改进

- 在导入业务事务内同步搜索索引；索引重建使用 savepoint，失败时保留已经
  验证的业务合并并安全回退 LIKE。
- 旧数据库按顺序迁移到 schema 2，不删除已有成员、群组或历史关系。
- 完成实际数据库迁移、完整分页性能复测和一致性备份/恢复候选演练。
- 消除 FTS 原型测试对单次微型计时结果的依赖，保留推荐逻辑与计时结果的
  一致性验证。
- FTS5 命中改为候选集合，并使用权威业务表的已转义 LIKE 条件精确复核，
  避免非 ASCII 大小写折叠产生与原搜索不同的额外结果。

### 兼容性

- 继续支持 Python 3.10 及以上版本。
- 自动获取数据、AstrBot 适配、HTTP 服务与 Docker 部署不属于本版本范围。

## 0.4.0 - 2026-08-24

### 新增

- 增加导入批次、来源哈希、重复跳过、强制重处理和关系观察记录。
- 增加顺序 schema 迁移、分页搜索、统计与导入历史查询。
- 增加 JSON、CSV、xlsx 导出、数据库健康检查和 SQLite 一致性在线备份。
- 增加 Python 3.10、3.12、3.14 持续集成。

### 修复与改进

- 保持非破坏性数据合并：新来源缺少成员时不删除历史关系，空值不覆盖旧值。
- 统一跨终端安全的 Unicode 命令行输出。
